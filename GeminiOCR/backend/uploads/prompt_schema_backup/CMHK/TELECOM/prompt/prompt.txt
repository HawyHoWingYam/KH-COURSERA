Please analyze the provided China Mobile (CMHK) Invoice PDF document and extract the information based on the FINAL UNIFIED SCHEMA provided.
CRITICAL INSTRUCTION: You MUST map the extracted data to the provided JSON schema. The key names in your output MUST EXACTLY match the key names defined in the schema (e.g., use "invoice_date", "customer_name", "service_number", etc.).
Overall Document Identification:
* provider: Set this value to "CMHK".
* __filename: Extract the filename of the PDF document.
I. Page 1 - Header & Account Information:
1. account_number:
   * Label: 'Customer No.'.
   * Value: Extract the number, which may include a decimal point (e.g., 1.22823751).
2. invoice_number:
   * Label: 'Invoice No.'.
   * Value: Extract the 11-digit number. This is a unique field for CMHK.
3. invoice_date:
   * Label: 'Issue Date'.
   * Value: Extract the date and convert to YYYY-MM-DD format.
4. customer_name:
   * Location: Top-left of the page.
   * Value: Extract the full company name in English.
5. payment_due_date:
   * Label: 'Payment Due On'.
   * Value: Extract the date from the payment slip section and convert to YYYY-MM-DD format.
II. Page 1 - Billing Summary (billing_summary object):
* previous_bill_amount:
   * Label: 'Opening Balance'.
   * Value: Positive number with 2 decimal places.
* payment_received:
   * Label: 'Payment Received - Thank You'.
   * Value: Negative number with 2 decimal places.
* balance_forwarded:
   * Label: 'Previous Balance'. This is the balance AFTER the payment was received.
   * Value: Number with 2 decimal places.
* total_amount_due:
   * Label: 'Total Amount Due'.
   * Value: The total amount for the current bill.
* current_charges:
   * Calculation: This value should be calculated as total_amount_due minus balance_forwarded.
III. Service & Charge Details (service_details array):
CRITICAL: The details are broken down by mobile number across multiple pages. You must iterate through each block.
For EACH mobile number block:
1. Create a new object in the service_details array and populate it:
   * service_number:
      * Label: 'Mobile Number'.
      * Value: Extract the number string. It can be a single number or two numbers separated by a slash (e.g., '53156812/14714300536').
   * user_name:
      * Label: 'User Name'.
      * Value: Extract the user's name if present. If not, the value should be null.
   * subtotal:
      * Label: 'Current Due' at the end of the charge list for that mobile number.
      * Value: The total for this specific mobile number.
   * charge_items (array):
      * Location: All line items under the 'Service Descriptions' for this mobile number.
      * For EACH line item, extract:
         * description: The full English text from the 'Service Descriptions' column.
         * period: The date range from the 'Date' column. Convert to 'YYYY/MM/DD-YYYY/MM/DD' format. If blank, the value should be null.
         * amount: The numeric value from the 'Amount(HK$)' column. CRITICAL: Preserve the sign. Discounts/waivers are negative.
IV. Final Validation Rule:
The sum of all subtotal fields from the service_details array should approximately equal the billing_summary.current_charges.