# FastAPI and web framework
fastapi==0.116.1
starlette==0.47.3
uvicorn[standard]==0.24.0
python-multipart==0.0.18

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Google Gemini AI
# Upgrade to support Gemini 2.x/2.5 models and structured output (response_schema)
# Keep within <0.9 to avoid breaking changes from the new unified SDK
google-generativeai>=0.7,<0.9

# Image processing
Pillow==10.3.0
opencv-python==4.8.1.78
pdf2image==1.17.0
PyMuPDF==1.23.20

# Data processing
pandas==2.1.4
numpy==1.25.2

# AWS Services
boto3==1.35.80
botocore==1.35.80

# Security updates (explicit pins for vulnerability fixes)
urllib3==2.5.0

# Configuration management
python-dotenv==1.0.0

# HTTP client
httpx==0.25.2

# Excel processing
openpyxl==3.1.2

# OneDrive Integration
O365==2.0.27

# Task Scheduling
APScheduler==3.10.4

# Testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
requests==2.32.4

# Standard library dependencies (already included in Python)
# asyncio, json, os, logging, time, uuid, datetime, tempfile, re, functools, urllib.parse, typing
